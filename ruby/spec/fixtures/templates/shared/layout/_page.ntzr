<!DOCTYPE html>
<html>
<head>
  <title>{[ page.title ]} - {[ site.title ]}</title>
</head>
<body>
{[!include /layout/header ]}
<main>
  <h2>{[ page.title ]}</h2>
  {[#if page.cards]}
  <div class="cards">
{[#each page.cards as card]}    {[!include /components/card title=card.title body=card.body ]}
{[/each]}  </div>
  {[/if]}
</main>
{[!include /layout/footer ]}
</body>
</html>
